const sumatory = (firstNumber, secondNumber) => +firstNumber + +secondNumber;
const substract = (firstNumber, secondNumber) => firstNumber - secondNumber;
const multiplication = (firstNumber, secondNumber) =>
  firstNumber * secondNumber;
const division = (firstNumber, secondNumber) => firstNumber / secondNumber;

module.exports = {
  sumatory,
  substract,
  multiplication,
  division,
};
